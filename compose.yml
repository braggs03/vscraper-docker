name: vScraper
services:
  backend:
    container_name: vScraper
    restart: always
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - ORIGINS=http://localhost:3000
      - VITE_API_URL=http://localhost:3000/api/